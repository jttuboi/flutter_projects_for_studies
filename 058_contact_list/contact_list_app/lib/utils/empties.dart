import 'package:flutter/material.dart';

class Empty {
  const Empty._();

  static const color = Colors.transparent;

  static const icon = Icons.abc;

  static final dateTime = DateTime.fromMillisecondsSinceEpoch(0);
}
