name: chat
description: "A new Flutter package project."
version: 0.0.1

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  # encrypt:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  # rethinkdb_dart:

flutter:
