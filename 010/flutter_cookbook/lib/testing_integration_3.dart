import 'package:flutter/material.dart';

class TestingIntegration3 extends StatelessWidget {
  const TestingIntegration3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
