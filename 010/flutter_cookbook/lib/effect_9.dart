import 'package:flutter/material.dart';

class Effect9 extends StatelessWidget {
  const Effect9({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
