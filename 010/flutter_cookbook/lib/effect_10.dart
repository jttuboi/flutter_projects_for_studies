import 'package:flutter/material.dart';

class Effect10 extends StatelessWidget {
  const Effect10({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
