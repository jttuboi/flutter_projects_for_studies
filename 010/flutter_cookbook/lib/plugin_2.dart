import 'package:flutter/material.dart';

class Plugin2 extends StatelessWidget {
  const Plugin2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
