import 'package:flutter/material.dart';

class Gesture1 extends StatelessWidget {
  const Gesture1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
