import 'package:flutter/material.dart';

class Networking1 extends StatelessWidget {
  const Networking1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
