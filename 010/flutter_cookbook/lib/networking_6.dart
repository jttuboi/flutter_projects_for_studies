import 'package:flutter/material.dart';

class Networking6 extends StatelessWidget {
  const Networking6({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
