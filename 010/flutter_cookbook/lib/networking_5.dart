import 'package:flutter/material.dart';

class Networking5 extends StatelessWidget {
  const Networking5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
