import 'package:flutter/material.dart';

class Pan3 extends StatelessWidget {
  const Pan3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
