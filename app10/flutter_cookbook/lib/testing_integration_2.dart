import 'package:flutter/material.dart';

class TestingIntegration2 extends StatelessWidget {
  const TestingIntegration2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
