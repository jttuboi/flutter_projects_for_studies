import 'package:flutter/material.dart';

class Effect7 extends StatelessWidget {
  const Effect7({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
