import 'package:flutter/material.dart';

class Networking2 extends StatelessWidget {
  const Networking2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
