import 'package:flutter/material.dart';

class Plugin1 extends StatelessWidget {
  const Plugin1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
