import 'package:flutter/material.dart';

class Gesture2 extends StatelessWidget {
  const Gesture2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
