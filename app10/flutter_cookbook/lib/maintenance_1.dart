import 'package:flutter/material.dart';

class Maintenance1 extends StatelessWidget {
  const Maintenance1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
