import 'package:flutter/material.dart';

class TestingWidget2 extends StatelessWidget {
  const TestingWidget2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
