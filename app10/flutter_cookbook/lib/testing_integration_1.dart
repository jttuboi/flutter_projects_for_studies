import 'package:flutter/material.dart';

class TestingIntegration1 extends StatelessWidget {
  const TestingIntegration1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
