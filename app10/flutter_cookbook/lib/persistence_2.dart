import 'package:flutter/material.dart';

class Persistence2 extends StatelessWidget {
  const Persistence2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
