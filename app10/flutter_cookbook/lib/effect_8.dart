import 'package:flutter/material.dart';

class Effect8 extends StatelessWidget {
  const Effect8({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
