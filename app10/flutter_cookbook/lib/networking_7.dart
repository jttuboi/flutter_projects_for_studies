import 'package:flutter/material.dart';

class Networking7 extends StatelessWidget {
  const Networking7({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
